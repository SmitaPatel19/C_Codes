#include <stdio.h>
int main()
{
    int m, n;
    printf("enter no. of rows : ");
    scanf("%d", &m);
    printf("enter no. of coloumns : ");
    scanf("%d", &n);
    for (int i = 1; i <= m; i++)
    {
        for (int j = 1; j <= m; j++)
        {
            
        }
        printf("\n");
    }
    return 0;
}